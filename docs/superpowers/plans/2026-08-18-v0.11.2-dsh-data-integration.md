# v0.11.2 DSH 数据接入实现计划（token 统计 + summary）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 DSH web 会话拥有与其他 AI CLI 对齐的 token 统计与 summary 能力，通过 loopback HTTP API 轮询 token、临时拉起 web 导出转录。

**Architecture:** 复用 0.11.1 已建立的 DSH loopback fetch 模式，新增 `dshWebClient` 封装 API；orchestrator 加 token 轮询器；`sessionHistoryService` 加 DSH 分支并经注入的 `exportDshHistory` 解析 web 地址（复用运行中或临时拉起）。

**Tech Stack:** Electron 43 / Node 24、Vue 3、`node:test`、`fflate`（ZIP 解压）、DSH loopback JSON-RPC API。

**Design spec:** `docs/superpowers/specs/2026-08-18-v0.11.2-dsh-data-integration-design.md`

---

## File Structure

- **Create** `electron/adapters/dshWebClient.js` — DSH loopback API 客户端（`listSessions` / `aggregateTokenUsage` / `exportSession`），含 loopback origin 校验与稳定错误码。
- **Create** `test/dsh-web-client.test.mjs` — 客户端单测（mock fetch）。
- **Modify** `electron/sessionHistory.js` — 新增 `parseDshHistory`（DSH session.jsonl → 归一化 items）。
- **Modify** `electron/adapters/adapterCapabilities.js` — `DSH_WEB_CAPABILITIES.statsOwner` → `'ucli'`。
- **Modify** `electron/usage/statsOwnership.js` — `sessionUsesUcliStats` 放宽 surface 条件。
- **Modify** `src/sessionMaintenancePresentation.js` — `coherentOwnership` web 分支放宽 stats。
- **Modify** `src/stores/sessions.js` — `rendererSessionCapabilities` 的 `nativeWeb` 放宽 stats。
- **Modify** `electron/orchestrator.js` — 装配 token 轮询器 + 注入 `exportDshHistory`。
- **Modify** `electron/sessionHistoryService.js` — DSH 分支 + `exportDshHistory` 注入。
- **Modify** `package.json` / `package-lock.json` — 引入 `fflate`。
- **Create** `test/dsh-stats-poller.test.mjs`、`test/dsh-history-service.test.mjs`（或并入现有）。
- **Modify** 现有测试：`test/adapter-capabilities.test.mjs`、`test/deepseek-harness-stats-ownership.test.mjs`、`test/deepseek-harness-presentation.test.mjs`。

---

## Task 1: capabilities 契约变更（stats 维度独立）

**Files:**
- Modify: `electron/adapters/adapterCapabilities.js`
- Modify: `electron/usage/statsOwnership.js`
- Modify: `src/sessionMaintenancePresentation.js`
- Modify: `src/stores/sessions.js`
- Test: `test/adapter-capabilities.test.mjs`、`test/deepseek-harness-stats-ownership.test.mjs`、`test/deepseek-harness-presentation.test.mjs`

- [ ] **Step 1: 写失败的契约测试**

改 `test/deepseek-harness-stats-ownership.test.mjs` 的 `WEB` 常量和断言（DSH web 的 stats 现在由 UCLI 拥有）：

```js
const WEB = {
  surface: 'web', permissionOwner: 'native', historyOwner: 'native',
  statsOwner: 'ucli', gateway: false, bridge: false
}

test('stats ownership accepts only authoritative UCLI-owned session capabilities', () => {
  assert.equal(sessionUsesUcliStats({ capabilities: TUI }), true)
  assert.equal(sessionUsesUcliStats({ capabilities: WEB }), true)
  assert.equal(sessionUsesUcliStats({}), false)
  assert.equal(sessionUsesUcliStats({ capabilities: undefined }), false)
  assert.equal(sessionUsesUcliStats({ capabilities: null }), false)
  // web surface 但 stats 由 native 管理（legacy 契约）仍然不是 UCLI 拥有
  assert.equal(sessionUsesUcliStats({ capabilities: { ...WEB, statsOwner: 'native' } }), false)
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/deepseek-harness-stats-ownership.test.mjs`
Expected: FAIL — `sessionUsesUcliStats(WEB)` 现返回 false。

- [ ] **Step 3: 实现——改 statsOwner + 放宽三处判定**

`electron/adapters/adapterCapabilities.js`（第 19-26 行）：

```js
export const DSH_WEB_CAPABILITIES = Object.freeze({
  surface: 'web',
  permissionOwner: 'native',
  historyOwner: 'native',
  statsOwner: 'ucli',
  gateway: false,
  bridge: false
})
```

`electron/usage/statsOwnership.js`（第 3-16 行，放宽 surface 条件，只依据 statsOwner）：

```js
export function sessionUsesUcliStats(session = {}) {
  if (!session.capabilities || typeof session.capabilities !== 'object' || Array.isArray(session.capabilities)) {
    return false
  }
  try {
    const capabilities = normalizeAdapterCapabilities(session.capabilities)
    return capabilities.statsOwner === 'ucli'
  } catch {
    return false
  }
}
```

`src/sessionMaintenancePresentation.js`（`deriveSessionCapabilityState` 内的 `coherentOwnership`，web 分支不再要求 stats 是 native）：

```js
const coherentOwnership = validShape && (
  capabilities.surface === 'terminal'
    ? capabilities.permissionOwner === 'ucli' && capabilities.historyOwner === 'ucli' && capabilities.statsOwner === 'ucli'
    : capabilities.permissionOwner === 'native' && capabilities.historyOwner === 'native'
)
```

`src/stores/sessions.js`（`rendererSessionCapabilities` 的 `nativeWeb`，去掉 statsOwner 必须 native）：

```js
const nativeWeb = normalized.surface === 'web' &&
  normalized.permissionOwner === 'native' && normalized.historyOwner === 'native' &&
  normalized.gateway === false && normalized.bridge === false
```

- [ ] **Step 4: 更新受影响的断言并跑测试**

`test/adapter-capabilities.test.mjs` 第 42-57 行把 DSH_WEB_CAPABILITIES 断言的 `statsOwner: 'native'` 改为 `'ucli'`。`test/deepseek-harness-presentation.test.mjs` 中 DSH web 的 `statsOwner: 'native'` 期望值同步改为 `'ucli'`。

Run: `node --test test/adapter-capabilities.test.mjs test/deepseek-harness-stats-ownership.test.mjs test/deepseek-harness-presentation.test.mjs test/dsh-web-only-migration.test.mjs test/session-maintenance-presentation.test.mjs test/skills-contracts.test.mjs`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add electron/adapters/adapterCapabilities.js electron/usage/statsOwnership.js src/sessionMaintenancePresentation.js src/stores/sessions.js test/adapter-capabilities.test.mjs test/deepseek-harness-stats-ownership.test.mjs test/deepseek-harness-presentation.test.mjs
git commit -m "feat(dsh): make UCLI own DSH web token stats"
```

---

## Task 2: 引入 fflate + dshWebClient 基础（listSessions / aggregateTokenUsage）

**Files:**
- Modify: `package.json`（`dependencies` 加 `"fflate"`）
- Create: `electron/adapters/dshWebClient.js`
- Test: `test/dsh-web-client.test.mjs`

- [ ] **Step 1: 写失败的客户端测试**

`test/dsh-web-client.test.mjs`：

```js
import assert from 'node:assert/strict'
import test from 'node:test'
import { createDshWebClient } from '../electron/adapters/dshWebClient.js'

function jsonFetch(handler) {
  return async (url, options) => {
    const method = url.slice(url.lastIndexOf('/api/') + '/api/'.length)
    const body = JSON.parse(options.body)
    return handler(method, body, { ok: true, json: async () => ({}) })
  }
}

test('aggregateTokenUsage sums uncachedInput+cacheRead into input and outputTokens into output', async () => {
  let sawPayload = null
  const fetchImpl = jsonFetch((method, body) => {
    sawPayload = { method, body }
    return {
      ok: true,
      json: async () => ({ result: { ok: true, value: {
        items: [
          { sessionId: 'a', updatedAt: 1, projections: { values: { tokenUsage: { uncachedInputTokens: 100, outputTokens: 50, cacheReadTokens: 20, cacheWriteTokens: 5 } } } },
          { sessionId: 'b', updatedAt: 2, projections: { values: { tokenUsage: { uncachedInputTokens: 1, outputTokens: 2, cacheReadTokens: 3, cacheWriteTokens: 4 } } } }
        ]
      } } } })
    }
  })
  const client = createDshWebClient({ fetchImpl })
  const result = await client.aggregateTokenUsage('http://127.0.0.1:43127')
  assert.deepEqual(result, { input: 124, output: 52 })
  assert.equal(sawPayload.method, 'session.list')
  assert.equal(sawPayload.body.method, 'session.list')
})

test('client rejects non-loopback origins and API errors with null', async () => {
  const client = createDshWebClient({ fetchImpl: async () => { throw new Error('unreachable') } })
  assert.equal(await client.aggregateTokenUsage('http://evil.example.com:43127'), null)
  const failing = createDshWebClient({ fetchImpl: async () => ({ ok: false, json: async () => ({}) }) })
  assert.equal(await failing.listSessions('http://127.0.0.1:43127'), null)
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/dsh-web-client.test.mjs`
Expected: FAIL — 模块不存在。

- [ ] **Step 3: 引入 fflate 并实现客户端**

```bash
npx npm@11.16.0 install fflate --save
```

`electron/adapters/dshWebClient.js`：

```js
import { randomUUID } from 'node:crypto'

const LOOPBACK_ORIGIN = /^http:\/\/127\.0\.0\.1:([1-9]\d{0,4})$/u

function isLoopbackOrigin(url) {
  const match = LOOPBACK_ORIGIN.exec(typeof url === 'string' ? url : '')
  if (!match) return false
  const port = Number(match[1])
  return port >= 1 && port <= 65_535
}

export function createDshWebClient({ fetchImpl = globalThis.fetch } = {}) {
  const request = async (url, method, payload) => {
    const response = await fetchImpl(`${url}/api/${method}`, {
      method: 'POST',
      headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ type: 'client-request', rpcId: randomUUID(), method, payload })
    })
    if (!response?.ok) return null
    const body = await response.json().catch(() => null)
    return body?.result?.ok === true ? body.result.value : null
  }

  async function listSessions(url) {
    if (!isLoopbackOrigin(url)) return null
    const value = await request(url, 'session.list', {})
    return Array.isArray(value?.items) ? value.items : null
  }

  async function aggregateTokenUsage(url) {
    const items = await listSessions(url)
    if (!items) return null
    let input = 0
    let output = 0
    for (const item of items) {
      const usage = item?.projections?.values?.tokenUsage
      if (!usage) continue
      input += Number(usage.uncachedInputTokens || 0) + Number(usage.cacheReadTokens || 0)
      output += Number(usage.outputTokens || 0)
    }
    return { input, output }
  }

  return { listSessions, aggregateTokenUsage }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test test/dsh-web-client.test.mjs`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json electron/adapters/dshWebClient.js test/dsh-web-client.test.mjs
git commit -m "feat(dsh): add DSH web client for session listing and token aggregation"
```

---

## Task 3: dshWebClient 导出 + parseDshHistory（fflate 解压 session.jsonl）

**Files:**
- Modify: `electron/adapters/dshWebClient.js`（加 `exportSession`）
- Modify: `electron/sessionHistory.js`（加 `parseDshHistory`）
- Test: `test/dsh-web-client.test.mjs`、`test/session-history.test.mjs`（或新建）

- [ ] **Step 1: 写失败测试**

在 `test/dsh-web-client.test.mjs` 加：

```js
import { unzipSync, zipSync } from 'fflate'

test('exportSession returns the session.jsonl text from the ZIP archive', async () => {
  const zipped = zipSync({ 'session.jsonl': new TextEncoder().encode('{"seq":1}\n{"seq":2}\n') })
  const fetchImpl = async (url) => {
    assert.equal(url, 'http://127.0.0.1:43127/api/session.export?sessionId=abc')
    return { ok: true, arrayBuffer: async () => zipped.buffer.slice(zipped.byteOffset, zipped.byteOffset + zipped.byteLength) }
  }
  const client = createDshWebClient({ fetchImpl })
  const text = await client.exportSession('http://127.0.0.1:43127', 'abc')
  assert.equal(text, '{"seq":1}\n{"seq":2}\n')
})
```

在 `test/session-history.test.mjs`（若存在）或新建 `test/session-history-dsh.test.mjs` 加：

```js
import { parseDshHistory } from '../electron/sessionHistory.js'

test('parseDshHistory maps user/assistant/tool events to normalized items', () => {
  const lines = [
    { seq: 1, type: 'user/message', time: 1000, data: { role: 'user', content: [{ type: 'text', text: 'hello' }] } },
    { seq: 2, type: 'assistant/message', time: 2000, data: { message: { content: [{ type: 'text', text: 'hi there' }] } } },
    { seq: 3, type: 'tool/result', time: 3000, data: { message: { content: [{ type: 'text', text: 'file written' }] } } },
    { seq: 4, type: 'request/header', time: 3000, data: {} }
  ]
  assert.deepEqual(parseDshHistory(lines.map(JSON.stringify)), [
    { id: '1', role: 'user', text: 'hello', timestamp: 1000000 },
    { id: '2', role: 'assistant', text: 'hi there', timestamp: 2000000 },
    { id: '3', role: 'tool', text: 'file written', timestamp: 3000000 }
  ])
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/dsh-web-client.test.mjs test/session-history-dsh.test.mjs`
Expected: FAIL — `exportSession` 与 `parseDshHistory` 不存在。

- [ ] **Step 3: 实现 exportSession（ZIP 解压）**

在 `electron/adapters/dshWebClient.js` 顶部加 `import { unzipSync } from 'fflate'`，并在 return 前加：

```js
  async function exportSession(url, sessionId) {
    if (!isLoopbackOrigin(url) || typeof sessionId !== 'string' || !sessionId) return null
    const response = await fetchImpl(`${url}/api/session.export?sessionId=${encodeURIComponent(sessionId)}`)
    if (!response?.ok) return null
    const buffer = await response.arrayBuffer().catch(() => null)
    if (!buffer) return null
    try {
      const files = unzipSync(new Uint8Array(buffer))
      const jsonl = files['session.jsonl']
      return jsonl ? new TextDecoder().decode(jsonl) : null
    } catch {
      return null
    }
  }
```

并把 `exportSession` 加入 return：`return { listSessions, aggregateTokenUsage, exportSession }`。

- [ ] **Step 4: 实现 parseDshHistory**

在 `electron/sessionHistory.js` 顶部附近（`parseCodexHistory` 等旁边）加：

```js
function dshMessageText(data) {
  const message = data?.message ?? data
  if (!message || typeof message !== 'object') return ''
  if (typeof message.content === 'string') return approvedText(message.content)
  return contentText(message.content)
}

export function parseDshHistory(lines = []) {
  const items = []
  for (const line of lines) {
    const record = parseRecord(line)
    if (!record || typeof record !== 'object' || typeof record.type !== 'string') continue
    const id = record.seq
    const time = record.time
    let role = null
    let text = ''
    if (record.type === 'user/message') {
      role = 'user'
      text = dshMessageText(record.data)
    } else if (record.type === 'assistant/message') {
      role = 'assistant'
      text = dshMessageText(record.data)
    } else if (record.type === 'tool/result') {
      role = 'tool'
      text = dshMessageText(record.data)
    }
    if (role) addItem(items, { id, role, text, timestamp: time })
  }
  return items
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `node --test test/dsh-web-client.test.mjs test/session-history-dsh.test.mjs`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add electron/adapters/dshWebClient.js electron/sessionHistory.js test/dsh-web-client.test.mjs test/session-history-dsh.test.mjs
git commit -m "feat(dsh): export DSH session log and parse to history items"
```

---

## Task 4: token 统计轮询器

**Files:**
- Modify: `electron/orchestrator.js`
- Test: `test/dsh-stats-poller.test.mjs`

- [ ] **Step 1: 写失败的轮询器测试**

轮询器设计为可独立测试的纯函数式组件，放 orchestrator 内但注入 `dshClient` 与定时器。`test/dsh-stats-poller.test.mjs`：

```js
import assert from 'node:assert/strict'
import test from 'node:test'
import { createDshStatsPoller } from '../electron/adapters/dshStatsPoller.js'

test('poller aggregates tokens for active DSH web sessions and skips inactive ones', async () => {
  const aggregated = []
  const entries = () => new Map([
    ['s1', { session: { adapterId: 'deepseek-harness' }, surfaceState: { url: 'http://127.0.0.1:43127' }, stats: { tokens: { input: 0, output: 0 } } }],
    ['s2', { session: { adapterId: 'deepseek-harness' }, surfaceState: null, stats: { tokens: { input: 0, output: 0 } } }]
  ])
  const poller = createDshStatsPoller({
    entries,
    dshClient: { aggregateTokenUsage: async (url) => { aggregated.push(url); return { input: 10, output: 4 } } },
    onStats: async (sessionId, tokens) => { /* captured below */ }
  })
  let called = []
  poller.onStats = async (sessionId, tokens) => called.push([sessionId, tokens])
  await poller.tick()
  assert.deepEqual(aggregated, ['http://127.0.0.1:43127'])
  assert.deepEqual(called, [['s1', { input: 10, output: 4 }]])
})

test('poller swallows client failures and keeps the session untouched', async () => {
  const poller = createDshStatsPoller({
    entries: () => new Map([['s1', { session: { adapterId: 'deepseek-harness' }, surfaceState: { url: 'http://127.0.0.1:43127' }, stats: { tokens: { input: 5, output: 5 } } }]]),
    dshClient: { aggregateTokenUsage: async () => { throw new Error('boom') } },
    onStats: async () => { throw new Error('must not fire') }
  })
  await poller.tick() // must not throw
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/dsh-stats-poller.test.mjs`
Expected: FAIL — 模块不存在。

- [ ] **Step 3: 实现轮询器**

`electron/adapters/dshStatsPoller.js`：

```js
const DEFAULT_INTERVAL_MS = 10_000

export function createDshStatsPoller({
  entries,
  dshClient,
  onStats,
  intervalMs = DEFAULT_INTERVAL_MS,
  setTimer = setTimeout,
  clearTimer = clearTimeout,
  now = () => Date.now()
} = {}) {
  let timer = null

  async function tick() {
    for (const [sessionId, entry] of entries()) {
      if (entry?.session?.adapterId !== 'deepseek-harness') continue
      const url = entry?.surfaceState?.url
      if (typeof url !== 'string' || !url) continue
      let tokens
      try {
        tokens = await dshClient.aggregateTokenUsage(url)
      } catch {
        continue
      }
      if (!tokens) continue
      try {
        await onStats(sessionId, tokens)
      } catch { /* observer failure must not break the loop */ }
    }
  }

  function start() {
    if (timer) return
    const loop = async () => {
      await tick()
      timer = setTimer(loop, intervalMs)
    }
    timer = setTimer(loop, intervalMs)
  }

  function stop() {
    if (timer !== null) clearTimer(timer)
    timer = null
  }

  return { tick, start, stop, now }
}
```

- [ ] **Step 4: 在 orchestrator 装配轮询器**

`electron/orchestrator.js` 的 `createOrchestrator` 内，创建 `dshWebClient` 与轮询器（在 `dshRuntimeManager` 初始化附近），`onStats` 写 `entry.stats.tokens`、`db.upsertStats` 并 `send('session:event', { sessionId, type: 'stats_update', usage: { inputTokens, outputTokens } })`。启动时 `start()`，shutdown 时 `stop()`。

- [ ] **Step 5: 跑测试确认通过**

Run: `node --test test/dsh-stats-poller.test.mjs`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add electron/adapters/dshStatsPoller.js electron/orchestrator.js test/dsh-stats-poller.test.mjs
git commit -m "feat(dsh): poll DSH web token usage into session stats"
```

---

## Task 5: summary 接入（sessionHistoryService DSH 分支 + exportDshHistory 临时拉起）

**Files:**
- Modify: `electron/sessionHistoryService.js`
- Modify: `electron/orchestrator.js`
- Test: `test/dsh-history-service.test.mjs`

- [ ] **Step 1: 写失败测试**

`test/dsh-history-service.test.mjs`：

```js
import assert from 'node:assert/strict'
import test from 'node:test'
import { createSessionHistoryService } from '../electron/sessionHistoryService.js'
import { parseDshHistory } from '../electron/sessionHistory.js'

test('DSH history loads via injected exporter and filters native sessions by updatedAt', async () => {
  const service = createSessionHistoryService({
    resolveSession: () => ({ id: 'u1', adapterId: 'deepseek-harness', cwd: 'C:/proj' }),
    exportDshHistory: async (session, { start, endExclusive }) => {
      assert.equal(start, 1000)
      assert.equal(endExclusive, 2000)
      return {
        items: parseDshHistory([
          JSON.stringify({ seq: 1, type: 'user/message', time: 1500, data: { content: [{ type: 'text', text: 'x' }] } })
        ]),
        sourceKind: 'export',
        sourceTruncated: false
      }
    }
  })
  const page = await service.loadRange({ sessionId: 'u1', start: 1000, endExclusive: 2000 })
  assert.equal(page.items.length, 1)
  assert.equal(page.items[0].role, 'user')
})

test('DSH history degrades when exporter returns null', async () => {
  const service = createSessionHistoryService({
    resolveSession: () => ({ id: 'u1', adapterId: 'deepseek-harness', cwd: 'C:/proj' }),
    exportDshHistory: async () => null
  })
  const page = await service.loadRange({ sessionId: 'u1', start: 1, endExclusive: 2 })
  assert.equal(page.missing, true)
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/dsh-history-service.test.mjs`
Expected: FAIL — `exportDshHistory` 未被识别、DSH 分支不存在。

- [ ] **Step 3: 实现 sessionHistoryService DSH 分支**

`electron/sessionHistoryService.js` 构造函数加 `exportDshHistory` 参数（默认 null），`loadProviderHistory` 加分支：

```js
  async function loadProviderHistory(session) {
    if (session.adapterId === 'deepseek-harness') {
      if (typeof exportDshHistory !== 'function') throw new Error('history provider unsupported')
      const history = await exportDshHistory(session, {})
      if (!history?.items) throw new Error('history source unavailable')
      return history
    }
    // ...existing claude/codex/opencode branches unchanged
  }
```

并在 `loadRange` 里跳过 `isSafeNativeSessionId` 门对 DSH 的处理（DSH 无 cliSessionId），改用 exporter 存在性门。

- [ ] **Step 4: 实现 orchestrator 注入 exportDshHistory（复用或临时拉起）**

`electron/orchestrator.js` 里 `createSessionHistoryService` 调用处加：

```js
    exportDshHistory: async (session, { start, endExclusive }) => {
      const url = entryFor(session.id)?.surfaceState?.url
      const client = getDshWebClient()
      const finish = async (activeUrl) => {
        const sessions = await client.listSessions(activeUrl)
        if (!sessions) return null
        const matched = sessions.filter((s) =>
          typeof s.updatedAt === 'number' &&
          s.updatedAt >= (start ?? 0) && s.updatedAt < (endExclusive ?? Infinity))
        const items = []
        for (const s of matched) {
          const text = await client.exportSession(activeUrl, s.sessionId)
          if (text) items.push(...parseDshHistory(text.split(/\r?\n/)))
        }
        return { items, sourceKind: 'export', sourceTruncated: false }
      }
      if (typeof url === 'string' && url) return finish(url)
      // 临时拉起：web 未运行时启动一个 DSH web，导出后关闭
      const selected = await getDshRuntimeManager().selectLaunch()
      if (!selected) return null
      const controller = launchDshWebSurface({
        runtime: selected,
        cwd: session.cwd,
        env: process.env,
        platform: process.platform
      })
      try {
        await controller.ready
        return await finish(controller.state.url)
      } catch {
        return null
      } finally {
        await controller.stop().catch(() => {})
      }
    }
```

（`entryFor` 是 orchestrator 内已有的 `sessions.get` 的封装；`getDshWebClient` 是新增的惰性单例，`parseDshHistory` 从 `./sessionHistory.js` 导入。）

- [ ] **Step 5: 跑测试确认通过**

Run: `node --test test/dsh-history-service.test.mjs test/deepseek-harness-web-surface.test.mjs`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add electron/sessionHistoryService.js electron/orchestrator.js test/dsh-history-service.test.mjs
git commit -m "feat(dsh): expose DSH session history for summaries"
```

---

## Task 6: 集成验证、版本与文档

**Files:**
- Modify: `package.json` / `package-lock.json`（版本 `0.11.2`）
- Modify: `CHANGELOG.md`
- Modify: `docs/protocol-reference.md`（DSH loopback API 使用）

- [ ] **Step 1: 跑全量测试**

Run: `npm test`
Expected: PASS（含 DSH 新测试 + 既有回归）

- [ ] **Step 2: 构建与打包验证**

Run:
```bash
npm run build
ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/" npm run dist:win
```
Expected: build 三目标通过，dist 产出 Setup/Portable。

- [ ] **Step 3: 更新版本与 CHANGELOG**

`package.json` 版本改 `0.11.2`，`CHANGELOG.md` 加 0.11.2 条目（DSH token 统计 + summary 接入）。`docs/protocol-reference.md` 补充 DSH loopback API 的 `session.list` / `session.export` 说明。

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json CHANGELOG.md docs/protocol-reference.md
git commit -m "chore(release): prepare UCLI 0.11.2 DSH data integration"
```

---

## Self-Review

- **Spec coverage:** 契约变更(Task 1)、dshWebClient 聚合(Task 2)、导出+解析(Task 3)、token 轮询(Task 4)、summary 临时拉起(Task 5)、版本文档(Task 6) 均覆盖。风险「fflate 引入」在 Task 2/3 落实。
- **Placeholder scan:** 无 TBD/TODO；`entryFor`/`getDshWebClient` 是 orchestrator 内明确新增的惰性访问器，Task 5 Step 4 已说明其含义。
- **Type consistency:** `aggregateTokenUsage` 返回 `{input, output}`、`exportSession` 返回 `string|null`、`parseDshHistory(lines: string[])` → `items[]` 在 Task 2-5 一致；`sessionUsesUcliStats` 新语义在 Task 1 与 `test/deepseek-harness-stats-ownership.test.mjs` 一致。
