model_provider = "work_gateway"
experimental_bearer_token = "decoy-bearer-must-not-leak"

[model_providers.work_gateway]
name = "Work Gateway"
base_url = "https://gateway.example.com/v1?decoy=must-not-leak"
env_key = "DECOY_SECRET_ENV_NAME"
http_headers = { Authorization = "Bearer static-secret-must-not-leak" }

[model_providers.local_ollama]
name = "Local Ollama"
base_url = "http://localhost:11434/v1"

[model_providers."unsafe;provider"]
name = "Ignored Provider"
